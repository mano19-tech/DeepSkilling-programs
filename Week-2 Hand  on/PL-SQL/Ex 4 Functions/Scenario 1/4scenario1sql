SET SERVEROUTPUT ON;
CREATE OR REPLACE FUNCTION CalculateAge (dob DATE)
RETURN NUMBER
IS
    age NUMBER;
BEGIN
    age := TRUNC(MONTHS_BETWEEN(SYSDATE, dob) / 12);
    RETURN age;
END;
/

SELECT CalculateAge(TO_DATE('1995-06-20', 'YYYY-MM-DD')) AS Age FROM DUAL;